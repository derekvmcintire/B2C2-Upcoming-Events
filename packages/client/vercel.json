{
  "version": 2,
  "rewrites": [
    { "source": "/submit", "destination": "/" }
  ],
  "routes": [
    { "handle": "filesystem" },
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}