run:
	npm run dev -- --host

run-api:
	vercel dev --yes

build:
	npm run build

lint:
	npm run lint

preview:
	npm run preview

typecheck:
	npm run typecheck

format:
	npm run format

add:
	npm run format; git add .

clean:
	npm run format; npm run typecheck; npm run lint